diff --git a/email_registration.module b/email_registration.module
index e12b5c9..f50c4cf 100644
--- a/email_registration.module
+++ b/email_registration.module
@@ -9,6 +9,13 @@
  * Implements hook_user_insert().
  */
 function email_registration_user_insert(&$edit, &$account, $category = NULL) {
+
+  // Don't alter username if one is already set.
+  // This is specificly to allow for tuakiri to set username first
+  if (!empty($account->name)) {
+    return;
+  }
+
   // Other modules may implement hook_email_registration_name($edit, $account)
   // to generate a username (return a string to be used as the username, NULL
   // to have email_registration generate it).
