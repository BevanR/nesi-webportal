default:
  context:
    parameters:
      drupal_users:
        researcher:
          username: 'behat-researcher'
          password: 'password'
  extensions:
    Behat\MinkExtension\Extension:
      base_url: 'http://************'
    Drupal\DrupalExtension\Extension:
      drush:
        alias: self
